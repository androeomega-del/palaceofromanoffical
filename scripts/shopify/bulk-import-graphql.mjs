#!/usr/bin/env node
// DISABLED by user policy: must never re-import BrandsGateway products
// or anything tagged `bg-import`. Out-of-stock products were manually
// removed from Shopify and must not be reactivated.
//
// The original implementation is preserved as
// `bulk-import-graphql.mjs.disabled` for reference only.
//
// To re-enable, you must first get explicit approval from the owner.
console.error('[disabled] BrandsGateway GraphQL bulk import is disabled by policy. See script header.');
process.exit(1);
