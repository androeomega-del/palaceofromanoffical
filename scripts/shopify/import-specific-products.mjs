#!/usr/bin/env node
// DISABLED by user policy: must never re-import BrandsGateway products
// (including anything that would carry the `bg-import` tag). Out-of-stock
// products were manually removed from Shopify and must not be reactivated.
//
// The original implementation is preserved as
// `import-specific-products.mjs.disabled` for reference only.
//
// To re-enable, you must first get explicit approval from the owner.
console.error('[disabled] BrandsGateway "import specific products" is disabled by policy. See script header.');
process.exit(1);
