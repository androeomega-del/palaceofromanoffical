In `src/components/home-studio/home-studio-layout.tsx`, remove the `<span aria-hidden="true">/</span>` separator between the "Shop Womenswear" and "Shop Menswear" links in the hero CTA row. The existing flex gap (`gap-x-6`) keeps the two links spaced cleanly without the slash.